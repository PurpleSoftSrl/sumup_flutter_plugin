name: sumup_example
description: Demonstrates how to use the sumup plugin.
version: 1.0.0+1


publish_to: 'none' 

environment:
  sdk: ^3.4.0

dependencies:
  flutter:
    sdk: flutter

  sumup:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
